# Documentation Audit Report and Roadmap

## 1. Build System Status
**Status:** 🔴 **Failing**
- **Issue:** The documentation build process uses `sphinx-build`, but critical source files referenced in `index.rst` are missing.
- **Specific Errors:**
    - `docs/index.rst` includes `reference/index` in its `toctree`, but `docs/reference/` contains only a `.gitignore` file.
    - `docs/index.rst` includes `auto_examples/index`, which is missing (likely previously generated by `sphinx-gallery`, which is currently not configured or run).
- **Environment:** `conf.py` is correctly configured to find the `dmipy_jax` package (adds `..` to `sys.path`), and `extensions` include `autodoc` and `myst_parser`.

## 2. Content Audit

### A. Landing Page (`docs/index.rst`)
- **Status:** ⚠️ Legacy
- **Findings:**
    - Refers to "dmipy" (legacy) rather than "dmipy-jax".
    - Links to legacy `README.md` on GitHub.
    - `toctree` points to broken paths (`auto_examples`, `reference`).
    - Does **not** link to the new JAX tutorials.

### B. API Reference (`docs/reference/`)
- **Status:** ❌ Missing
- **Findings:**
    - Directory is empty (except `.gitignore`).
    - No `rst` files exist to trigger `autodoc` for `dmipy_jax` modules.

### C. Tutorials (`docs/tutorials/`)
- **Status:** ✅ Valid but Orphaned
- **Findings:**
    - `first_steps.md`: Excellent, up-to-date guide for `dmipy-jax` covering `JaxAcquisition`, `C1Stick`, `vmap`, and `jit`.
    - **Issue:** This file is **not linked** in `docs/index.rst`'s `toctree`, making it inaccessible in the built HTML.

### D. Theory (`docs/theory.rst`)
- **Status:** ⚠️ Placeholder
- **Findings:**
    - Contains generic placeholder text ("This is a theory section") and a dummy equation.
    - Needs to be updated or removed until real JAX-specific theory (e.g., GPU parallelization model) is ready.

## 3. Documentation Roadmap

The goal is to restore a working build and document the new `dmipy_jax` architecture.

### Phase 1: Fix Build & Structure (High Priority)
*Objective: Get `uv run sphinx-build` to pass.*
1.  **Update `docs/index.rst`**:
    - Remove `auto_examples/index` from `toctree`.
    - Remove `reference/index` (temporarily) or create the file.
    - Add `tutorials/first_steps` to `toctree`.
    - Rename "Welcome to dmipy's documentation!" to "Welcome to dmipy-jax information".
2.  **Create `docs/reference/index.rst`**:
    - Create a skeleton file to allow the build to pass if kept in `toctree`.

### Phase 2: API Documentation (High Priority)
*Objective: Auto-generate API reference for core modules.*
1.  **Populate `docs/reference/index.rst`**:
    - Add `toctree` entries for submodules.
2.  **Create Module Pages**:
    - `docs/reference/core.rst`: `.. automodule:: dmipy_jax.core`
    - `docs/reference/acquisition.rst`: `.. automodule:: dmipy_jax.acquisition`
    - `docs/reference/models.rst`: `.. automodule:: dmipy_jax.models`
    - `docs/reference/signal_models.rst`: `.. automodule:: dmipy_jax.signal_models`
    - `docs/reference/fitting.rst`: `.. automodule:: dmipy_jax.fitting`

### Phase 3: Content Polish (Medium Priority)
*Objective: Make the docs useful for users.*
1.  **Expand `tutorials/first_steps.md`**:
    - Ensure it runs as a valid Myst notebook or keep as markdown code blocks (current state is fine for now).
2.  **Update `conf.py`**:
    - Ensure `html_title` and `project` metadata are consistent.
3.  **Clean up `docs/theory.rst`**:
    - Either remove it or add a brief section on "Why JAX?" (Vectorization, GPU support).

## 4. Immediate Next Steps
The user should approve the creation of the missing `reference/*.rst` files and the modification of `index.rst` to fix the broken build.
